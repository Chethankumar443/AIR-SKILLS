# React AI System Instructions

## Component Design
- Always write React components as typed functional components.
- Use explicit prop interfaces ending with `Props` (e.g., `ButtonProps`).
- Avoid default exports; use named exports for all components.

## Styling Rules
- Use Tailwind CSS utility classes exclusively.
- Prefer HSL color tokens for seamless dark mode support.
