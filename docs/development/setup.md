# Developer Setup Guide

1. Install Rust 1.80+ (`rustup`).
2. Clone repository: `git clone https://github.com/AIR-SKILLS/air-skills`.
3. Check build: `cargo check --workspace --all-targets`.
4. Run tests: `cargo test --workspace`.
5. Run CLI: `cargo run -p air-cli -- --help`.
